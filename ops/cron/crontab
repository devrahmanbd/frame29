# GENERATED by scripts/gen-crontab.mjs — do not edit by hand.
# Source of truth: src/lib/cron-registry.ts. Regenerate with `bun run cron:generate`.
#
# Runs inside the supercronic container (ops/docker-compose.cron.yml), which
# reads APP_INTERNAL_URL and BILLING_CRON_SECRET from ops/.env.
# Each job answers 200 (ran), 409 (another scheduler holds the lease) or 5xx;
# supercronic logs the status line and Promtail ships it to Loki.
SHELL=/bin/sh

# Queue worker tick
* * * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron jobs status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/jobs"

# Outbound webhook dispatch
*/2 * * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron webhooks status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/webhooks"

# Notification fan-out
*/5 * * * * curl -sS -X POST -m 50 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron notifications status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/notifications"

# Courier tracking poll
7,37 * * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron couriers status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/couriers"

# Payout sweep
12 * * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron payouts status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/payouts"

# Subscription billing run
20 2 * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron billing status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/billing"

# Reliability sweep
40 3 * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron ops status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/ops"

# Data purge
55 3 * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron purge status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/purge"

# Analytics rollup
25 * * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron analytics status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/analytics"

# Ad integrity sweep
35 * * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron ad-fraud status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/ad-fraud"

# Growth automations
*/15 * * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron growth status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/growth"

# Support SLA sweep
*/20 * * * * curl -sS -X POST -m 50 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron support status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/support"

# Custom domain sweep
50 */2 * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron domains status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/domains"

# Marketplace theme sweep
45 4 * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron themes status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/themes"

# Search Console refresh
15 5 * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron search-console status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/search-console"

# Content health scan
30 6 * * * curl -sS -X POST -m 60 --retry 1 --retry-delay 5 -H "authorization: Bearer $BILLING_CRON_SECRET" -H "x-cron-trigger: supercronic" -o /dev/null -w 'cron content-health status=%{http_code} t=%{time_total}s\n' "$APP_INTERNAL_URL/api/public/cron/content-health"
